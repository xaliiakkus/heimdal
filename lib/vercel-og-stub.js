/**
 * Stub for @vercel/og / next/dist/compiled/@vercel/og.
 * Proje OG image üretmediği için Worker bundle'dan çıkarıyoruz (Cloudflare 3 MiB limiti).
 */
function ImageResponse() {
  return null;
}
module.exports = { ImageResponse };
module.exports.ImageResponse = ImageResponse;
